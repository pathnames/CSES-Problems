#include <bits/stdc++.h>
typedef long long ll;
using namespace std;


int main()
{
    int n;
    cin >> n; 

    vector<vector<int>> opt;
    //(n+1)*(n+1) 2D array to tabulate.
    opt.resize(n); 
    for(auto vec: opt) vec.resize(n); 
    



    return 0;
}